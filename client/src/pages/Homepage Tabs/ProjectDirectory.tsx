import React from "react";
import Wideo from "../../static/util/Wideo";
import Post from "./Post";

const ProjectDirectory: React.FC = () => {
	return <div className="ProjectDirectory">{posts()}</div>
};

const posts: React.FC[] = [
		<>{/*this will be like a grid with image links to different parts of the site like /langton and /fridgE */}</>,
		<>{/*maybe move the work experience stuff here eventually*/}</>
];
			
		

export default GamingTab;